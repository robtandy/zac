{
  "name": "zac-web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=esm --target=es2022 --minify && cp src/index.html dist/index.html && cp src/styles.css dist/styles.css && cp src/canvas-host.html dist/canvas-host.html",
    "watch": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=esm --target=es2022 --watch"
  },
  "dependencies": {
    "marked": "^15.0.0",
    "marked-highlight": "^2.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0",
    "@types/dompurify": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
